name: Build Design Tokens

on:
  push:
    branches:
      - main
    paths:
      - "tokens/**"
      - "config/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-tokens:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          
      - name: Install dependencies
        run: npm install style-dictionary

      - name: Build tokens
        run: |
          node scripts/prepare-tokens.cjs
          npx style-dictionary build --config config/style-dictionary.config.json

      - name: Commit generated CSS
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "build: update generated token css"
          file_pattern: "build/css/variables.css config/generated.tokens.json"