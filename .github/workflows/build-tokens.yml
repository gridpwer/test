name: Build Design Tokens

on:
  push:
    branches:
      - main
    paths:
      - "tokens/**"
      - "config/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-tokens:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          
      - name: Install dependencies
        run: npm ci

      - name: Build tokens
        run: npm run build

      - name: Commit generated tokens
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "build: update generated tokens (css, md, mdx)"
          file_pattern: "build/css/variables.css build/docs/design-tokens.md build/docs/tokens.mdx config/generated.tokens.json"